{
  "name": "landsat-web-tracker",
  "private": true,
  "scripts": {
    "frontend:dev": "pnpm --prefix ./frontend/ dev",
    "frontend:lint": "pnpm --prefix ./frontend/ lint",
    "frontend:fix": "pnpm --prefix ./frontend/ fix",
    "backend:dev": "cd backend && poetry run uvicorn src.main:app --reload",
    "dev": "pnpm run /:dev$/",
    "lint": "pnpm run /:lint$/",
    "postinstall": "poetry install",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.6"
  }
}
